post:
  summary: Mark Notifications as Read
  description: Mark all notifications as read.
  tags:
    - notifications
  security:
    - userId: []
      apiToken: []
      xClient: []
  responses:
    '200':
      description: >-
        OK (Notifications marked as read successfully, returns
        user.notifications)
      content:
        application/json:
          schema:
            $ref: ../components/schemas/DataResponse.yaml
    '401':
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ErrorMessageResponse.yaml
