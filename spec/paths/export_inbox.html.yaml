servers:
  - url: https://habitica.com
get:
  summary: Export Inbox Data in HTML
  description: Export user private messages as HTML document.
  tags:
    - data-export
  security:
    - userId: []
      apiToken: []
      xClient: []
  responses:
    '200':
      description: OK
      content:
        text/html:
          schema:
            type: string
            format: binary
    '401':
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ErrorMessageResponse.yaml
