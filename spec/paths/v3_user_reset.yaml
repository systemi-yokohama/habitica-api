post:
  summary: Reset user
  description: >
    Reset the authenticated user's data, removing all progress and starting
    over.  \

    See: [ApiDoc](https://habitica.com/apidoc/#api-User-UserReset)
  tags:
    - user
  security:
    - userId: []
      apiToken: []
      xClient: []
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: ../components/schemas/DataResponse.yaml
    '401':
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ErrorMessageResponse.yaml
